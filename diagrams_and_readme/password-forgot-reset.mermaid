sequenceDiagram
    actor User
    User ->> AuthenticationController: POST /api/v1/auth/password/forgot/reset
    AuthenticationController ->> PasswordService: resetPassword(PasswordResetConfirmRequest)
    PasswordService -->> AuthenticationController: completion
    AuthenticationController -->> User: 200 ApiResponse {message: "password reset successful"}
