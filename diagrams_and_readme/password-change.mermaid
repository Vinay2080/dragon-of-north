sequenceDiagram
    actor User
    User ->> AuthenticationController: POST /api/v1/auth/password/change
    AuthenticationController ->> PasswordService: changePassword(PasswordChangeRequest)
    PasswordService -->> AuthenticationController: completion
    AuthenticationController -->> User: 200 ApiResponse {message: "password change successful"}
